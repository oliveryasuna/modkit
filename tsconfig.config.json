{
  "extends": "./tsconfig.json",
  "include": [
    "./*.ts"
  ],
  "compilerOptions": {
    //==================================================
    // Modules
    //==================================================

    "types": [
      "bun"
    ],

    //==================================================
    // Emit
    //==================================================

    "declaration": false,
    "declarationMap": false
  }
}
